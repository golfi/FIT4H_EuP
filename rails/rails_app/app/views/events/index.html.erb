<h1>Events</h1>													
<p><%= link_to "Neues Event anlegen", new_event_path %></p>
<p> Zeige folgende <%= @events.size %> Events an:
	<table width="100%">
		<tr>
			<th>Name</th>
			<th>Ort</th>
			<th>Preis</th>
			<th>wann</th>
			<th>Beschreibung</th>
			<th>Show</th>
			<th>Edit</th>
			<th>Del</th>
		</tr>
		<% @events.each do |event| %>
		<tr>
			<td><%= event.name %></td>
			<td><%= event.location %></td>
			<td><%= format_price(event) %></td>
			<td><%= event.start_at %></td>
			<td>
					<%= truncate(event.description, length: 8, separator: ' ', omission: "") %>
					<%= link_to "...", event_path(event.id) %>
			</td>
			<td><%= link_to "Show", event_path(event.id)%></td>
			<td><%= link_to "Edit", edit_event_path(event.id) %></td>
			<td><%= link_to "Delete", event_path(event.id), method: :delete, data: {confirm: "Sicher???"} %></td>
		</tr>
		<% end %>
	</table>
	Wenn Sie alle Datenbankeinträge sehen wollen, klicken Sie auf den nachstehenden Link!
	<br><br><p><%= link_to "Alle Datenbankeinträge", daten_events_path, type: "button", class: "button success tiny" %></p>
</p>

	<!--<td><%#= format_price(event) %></td>--> <!--So wird auskommentiert-->